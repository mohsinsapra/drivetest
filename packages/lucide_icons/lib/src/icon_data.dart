import 'package:flutter/widgets.dart';

// Flutter 3.27+ made IconData final, so it can no longer be extended.
// Alias kept for source compatibility with any code that references LucideIconData as a type.
typedef LucideIconData = IconData;
